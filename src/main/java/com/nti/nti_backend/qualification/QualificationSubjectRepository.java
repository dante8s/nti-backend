package com.nti.nti_backend.qualification;

import org.springframework.data.jpa.repository.JpaRepository;

public interface QualificationSubjectRepository extends JpaRepository<QualificationSubject, Long> {
}
